var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("elm", require("highlight.js/lib/languages/elm.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "elm",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "elm" },
      React.createElement('code', {
        className: 'hljs ' + "elm",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("elm", children, true).value
        }
      })
    );
  }
});
