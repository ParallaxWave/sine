var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("erlang", require("highlight.js/lib/languages/erlang.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "erlang",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "erlang" },
      React.createElement('code', {
        className: 'hljs ' + "erlang",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("erlang", children, true).value
        }
      })
    );
  }
});
