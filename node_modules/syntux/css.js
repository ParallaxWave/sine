var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("css", require("highlight.js/lib/languages/css.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "css",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "css" },
      React.createElement('code', {
        className: 'hljs ' + "css",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("css", children, true).value
        }
      })
    );
  }
});
