var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("elixir", require("highlight.js/lib/languages/elixir.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "elixir",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "elixir" },
      React.createElement('code', {
        className: 'hljs ' + "elixir",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("elixir", children, true).value
        }
      })
    );
  }
});
