var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("julia", require("highlight.js/lib/languages/julia.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "julia",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "julia" },
      React.createElement('code', {
        className: 'hljs ' + "julia",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("julia", children, true).value
        }
      })
    );
  }
});
