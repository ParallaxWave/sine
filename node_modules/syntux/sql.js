var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("sql", require("highlight.js/lib/languages/sql.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "sql",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "sql" },
      React.createElement('code', {
        className: 'hljs ' + "sql",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("sql", children, true).value
        }
      })
    );
  }
});
