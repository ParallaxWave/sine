var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("prolog", require("highlight.js/lib/languages/prolog.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "prolog",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "prolog" },
      React.createElement('code', {
        className: 'hljs ' + "prolog",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("prolog", children, true).value
        }
      })
    );
  }
});
