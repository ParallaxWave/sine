var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("roboconf", require("highlight.js/lib/languages/roboconf.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "roboconf",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "roboconf" },
      React.createElement('code', {
        className: 'hljs ' + "roboconf",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("roboconf", children, true).value
        }
      })
    );
  }
});
