var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("erb", require("highlight.js/lib/languages/erb.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "erb",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "erb" },
      React.createElement('code', {
        className: 'hljs ' + "erb",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("erb", children, true).value
        }
      })
    );
  }
});
