var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("lua", require("highlight.js/lib/languages/lua.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "lua",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "lua" },
      React.createElement('code', {
        className: 'hljs ' + "lua",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("lua", children, true).value
        }
      })
    );
  }
});
