var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("nsis", require("highlight.js/lib/languages/nsis.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "nsis",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "nsis" },
      React.createElement('code', {
        className: 'hljs ' + "nsis",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("nsis", children, true).value
        }
      })
    );
  }
});
