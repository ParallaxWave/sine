var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("makefile", require("highlight.js/lib/languages/makefile.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "makefile",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "makefile" },
      React.createElement('code', {
        className: 'hljs ' + "makefile",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("makefile", children, true).value
        }
      })
    );
  }
});
