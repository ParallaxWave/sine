var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("smali", require("highlight.js/lib/languages/smali.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "smali",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "smali" },
      React.createElement('code', {
        className: 'hljs ' + "smali",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("smali", children, true).value
        }
      })
    );
  }
});
