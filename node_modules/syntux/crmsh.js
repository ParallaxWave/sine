var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("crmsh", require("highlight.js/lib/languages/crmsh.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "crmsh",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "crmsh" },
      React.createElement('code', {
        className: 'hljs ' + "crmsh",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("crmsh", children, true).value
        }
      })
    );
  }
});
