var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("mathematica", require("highlight.js/lib/languages/mathematica.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "mathematica",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "mathematica" },
      React.createElement('code', {
        className: 'hljs ' + "mathematica",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("mathematica", children, true).value
        }
      })
    );
  }
});
