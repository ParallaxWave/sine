var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("r", require("highlight.js/lib/languages/r.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "r",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "r" },
      React.createElement('code', {
        className: 'hljs ' + "r",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("r", children, true).value
        }
      })
    );
  }
});
