var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("ini", require("highlight.js/lib/languages/ini.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "ini",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "ini" },
      React.createElement('code', {
        className: 'hljs ' + "ini",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("ini", children, true).value
        }
      })
    );
  }
});
