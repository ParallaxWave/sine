var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("gams", require("highlight.js/lib/languages/gams.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "gams",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "gams" },
      React.createElement('code', {
        className: 'hljs ' + "gams",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("gams", children, true).value
        }
      })
    );
  }
});
