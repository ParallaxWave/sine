var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("go", require("highlight.js/lib/languages/go.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "go",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "go" },
      React.createElement('code', {
        className: 'hljs ' + "go",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("go", children, true).value
        }
      })
    );
  }
});
