var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("accesslog", require("highlight.js/lib/languages/accesslog.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "accesslog",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "accesslog" },
      React.createElement('code', {
        className: 'hljs ' + "accesslog",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("accesslog", children, true).value
        }
      })
    );
  }
});
