var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("sml", require("highlight.js/lib/languages/sml.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "sml",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "sml" },
      React.createElement('code', {
        className: 'hljs ' + "sml",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("sml", children, true).value
        }
      })
    );
  }
});
