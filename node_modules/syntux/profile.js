var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("profile", require("highlight.js/lib/languages/profile.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "profile",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "profile" },
      React.createElement('code', {
        className: 'hljs ' + "profile",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("profile", children, true).value
        }
      })
    );
  }
});
