var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("gradle", require("highlight.js/lib/languages/gradle.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "gradle",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "gradle" },
      React.createElement('code', {
        className: 'hljs ' + "gradle",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("gradle", children, true).value
        }
      })
    );
  }
});
