var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("lasso", require("highlight.js/lib/languages/lasso.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "lasso",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "lasso" },
      React.createElement('code', {
        className: 'hljs ' + "lasso",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("lasso", children, true).value
        }
      })
    );
  }
});
