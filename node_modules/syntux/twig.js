var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("twig", require("highlight.js/lib/languages/twig.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "twig",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "twig" },
      React.createElement('code', {
        className: 'hljs ' + "twig",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("twig", children, true).value
        }
      })
    );
  }
});
