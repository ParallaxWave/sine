var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("applescript", require("highlight.js/lib/languages/applescript.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "applescript",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "applescript" },
      React.createElement('code', {
        className: 'hljs ' + "applescript",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("applescript", children, true).value
        }
      })
    );
  }
});
