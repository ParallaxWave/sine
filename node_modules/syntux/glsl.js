var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("glsl", require("highlight.js/lib/languages/glsl.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "glsl",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "glsl" },
      React.createElement('code', {
        className: 'hljs ' + "glsl",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("glsl", children, true).value
        }
      })
    );
  }
});
