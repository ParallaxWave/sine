var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("kotlin", require("highlight.js/lib/languages/kotlin.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "kotlin",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "kotlin" },
      React.createElement('code', {
        className: 'hljs ' + "kotlin",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("kotlin", children, true).value
        }
      })
    );
  }
});
