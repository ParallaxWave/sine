var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("vim", require("highlight.js/lib/languages/vim.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "vim",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "vim" },
      React.createElement('code', {
        className: 'hljs ' + "vim",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("vim", children, true).value
        }
      })
    );
  }
});
