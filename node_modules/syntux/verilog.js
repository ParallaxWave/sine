var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("verilog", require("highlight.js/lib/languages/verilog.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "verilog",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "verilog" },
      React.createElement('code', {
        className: 'hljs ' + "verilog",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("verilog", children, true).value
        }
      })
    );
  }
});
