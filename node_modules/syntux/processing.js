var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("processing", require("highlight.js/lib/languages/processing.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "processing",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "processing" },
      React.createElement('code', {
        className: 'hljs ' + "processing",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("processing", children, true).value
        }
      })
    );
  }
});
