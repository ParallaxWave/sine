var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("pf", require("highlight.js/lib/languages/pf.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "pf",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "pf" },
      React.createElement('code', {
        className: 'hljs ' + "pf",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("pf", children, true).value
        }
      })
    );
  }
});
