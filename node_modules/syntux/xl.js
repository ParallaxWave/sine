var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("xl", require("highlight.js/lib/languages/xl.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "xl",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "xl" },
      React.createElement('code', {
        className: 'hljs ' + "xl",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("xl", children, true).value
        }
      })
    );
  }
});
