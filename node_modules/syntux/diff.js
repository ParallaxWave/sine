var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("diff", require("highlight.js/lib/languages/diff.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "diff",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "diff" },
      React.createElement('code', {
        className: 'hljs ' + "diff",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("diff", children, true).value
        }
      })
    );
  }
});
