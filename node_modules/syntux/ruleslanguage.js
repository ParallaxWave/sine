var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("ruleslanguage", require("highlight.js/lib/languages/ruleslanguage.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "ruleslanguage",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "ruleslanguage" },
      React.createElement('code', {
        className: 'hljs ' + "ruleslanguage",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("ruleslanguage", children, true).value
        }
      })
    );
  }
});
