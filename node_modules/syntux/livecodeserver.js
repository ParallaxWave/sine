var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("livecodeserver", require("highlight.js/lib/languages/livecodeserver.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "livecodeserver",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "livecodeserver" },
      React.createElement('code', {
        className: 'hljs ' + "livecodeserver",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("livecodeserver", children, true).value
        }
      })
    );
  }
});
