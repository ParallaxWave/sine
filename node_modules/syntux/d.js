var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("d", require("highlight.js/lib/languages/d.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "d",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "d" },
      React.createElement('code', {
        className: 'hljs ' + "d",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("d", children, true).value
        }
      })
    );
  }
});
