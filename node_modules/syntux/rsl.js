var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("rsl", require("highlight.js/lib/languages/rsl.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "rsl",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "rsl" },
      React.createElement('code', {
        className: 'hljs ' + "rsl",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("rsl", children, true).value
        }
      })
    );
  }
});
