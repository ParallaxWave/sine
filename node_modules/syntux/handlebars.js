var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("handlebars", require("highlight.js/lib/languages/handlebars.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "handlebars",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "handlebars" },
      React.createElement('code', {
        className: 'hljs ' + "handlebars",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("handlebars", children, true).value
        }
      })
    );
  }
});
