var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("openscad", require("highlight.js/lib/languages/openscad.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "openscad",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "openscad" },
      React.createElement('code', {
        className: 'hljs ' + "openscad",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("openscad", children, true).value
        }
      })
    );
  }
});
