var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("armasm", require("highlight.js/lib/languages/armasm.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "armasm",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "armasm" },
      React.createElement('code', {
        className: 'hljs ' + "armasm",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("armasm", children, true).value
        }
      })
    );
  }
});
