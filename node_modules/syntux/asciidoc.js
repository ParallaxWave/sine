var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("asciidoc", require("highlight.js/lib/languages/asciidoc.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "asciidoc",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "asciidoc" },
      React.createElement('code', {
        className: 'hljs ' + "asciidoc",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("asciidoc", children, true).value
        }
      })
    );
  }
});
