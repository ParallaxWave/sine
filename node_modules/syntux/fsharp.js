var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("fsharp", require("highlight.js/lib/languages/fsharp.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "fsharp",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "fsharp" },
      React.createElement('code', {
        className: 'hljs ' + "fsharp",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("fsharp", children, true).value
        }
      })
    );
  }
});
