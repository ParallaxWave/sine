var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("json", require("highlight.js/lib/languages/json.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "json",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "json" },
      React.createElement('code', {
        className: 'hljs ' + "json",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("json", children, true).value
        }
      })
    );
  }
});
