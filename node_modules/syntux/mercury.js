var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("mercury", require("highlight.js/lib/languages/mercury.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "mercury",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "mercury" },
      React.createElement('code', {
        className: 'hljs ' + "mercury",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("mercury", children, true).value
        }
      })
    );
  }
});
