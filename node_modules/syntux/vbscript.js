var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("vbscript", require("highlight.js/lib/languages/vbscript.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "vbscript",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "vbscript" },
      React.createElement('code', {
        className: 'hljs ' + "vbscript",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("vbscript", children, true).value
        }
      })
    );
  }
});
