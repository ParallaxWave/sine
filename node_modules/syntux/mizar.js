var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("mizar", require("highlight.js/lib/languages/mizar.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "mizar",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "mizar" },
      React.createElement('code', {
        className: 'hljs ' + "mizar",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("mizar", children, true).value
        }
      })
    );
  }
});
