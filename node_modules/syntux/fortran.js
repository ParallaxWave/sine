var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("fortran", require("highlight.js/lib/languages/fortran.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "fortran",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "fortran" },
      React.createElement('code', {
        className: 'hljs ' + "fortran",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("fortran", children, true).value
        }
      })
    );
  }
});
