var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("smalltalk", require("highlight.js/lib/languages/smalltalk.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "smalltalk",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "smalltalk" },
      React.createElement('code', {
        className: 'hljs ' + "smalltalk",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("smalltalk", children, true).value
        }
      })
    );
  }
});
