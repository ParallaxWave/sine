var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("clojure-repl", require("highlight.js/lib/languages/clojure-repl.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "clojure-repl",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "clojure-repl" },
      React.createElement('code', {
        className: 'hljs ' + "clojure-repl",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("clojure-repl", children, true).value
        }
      })
    );
  }
});
