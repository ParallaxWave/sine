var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("php", require("highlight.js/lib/languages/php.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "php",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "php" },
      React.createElement('code', {
        className: 'hljs ' + "php",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("php", children, true).value
        }
      })
    );
  }
});
