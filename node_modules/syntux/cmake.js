var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("cmake", require("highlight.js/lib/languages/cmake.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "cmake",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "cmake" },
      React.createElement('code', {
        className: 'hljs ' + "cmake",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("cmake", children, true).value
        }
      })
    );
  }
});
