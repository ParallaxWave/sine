var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("dust", require("highlight.js/lib/languages/dust.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "dust",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "dust" },
      React.createElement('code', {
        className: 'hljs ' + "dust",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("dust", children, true).value
        }
      })
    );
  }
});
