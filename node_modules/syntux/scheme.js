var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("scheme", require("highlight.js/lib/languages/scheme.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "scheme",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "scheme" },
      React.createElement('code', {
        className: 'hljs ' + "scheme",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("scheme", children, true).value
        }
      })
    );
  }
});
