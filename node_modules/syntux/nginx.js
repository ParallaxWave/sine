var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("nginx", require("highlight.js/lib/languages/nginx.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "nginx",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "nginx" },
      React.createElement('code', {
        className: 'hljs ' + "nginx",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("nginx", children, true).value
        }
      })
    );
  }
});
