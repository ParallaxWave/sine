var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("swift", require("highlight.js/lib/languages/swift.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "swift",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "swift" },
      React.createElement('code', {
        className: 'hljs ' + "swift",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("swift", children, true).value
        }
      })
    );
  }
});
