var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("xquery", require("highlight.js/lib/languages/xquery.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "xquery",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "xquery" },
      React.createElement('code', {
        className: 'hljs ' + "xquery",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("xquery", children, true).value
        }
      })
    );
  }
});
