var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("mel", require("highlight.js/lib/languages/mel.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "mel",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "mel" },
      React.createElement('code', {
        className: 'hljs ' + "mel",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("mel", children, true).value
        }
      })
    );
  }
});
