var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("dockerfile", require("highlight.js/lib/languages/dockerfile.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "dockerfile",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "dockerfile" },
      React.createElement('code', {
        className: 'hljs ' + "dockerfile",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("dockerfile", children, true).value
        }
      })
    );
  }
});
