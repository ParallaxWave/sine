var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("tex", require("highlight.js/lib/languages/tex.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "tex",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "tex" },
      React.createElement('code', {
        className: 'hljs ' + "tex",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("tex", children, true).value
        }
      })
    );
  }
});
