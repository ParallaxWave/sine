var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("q", require("highlight.js/lib/languages/q.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "q",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "q" },
      React.createElement('code', {
        className: 'hljs ' + "q",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("q", children, true).value
        }
      })
    );
  }
});
