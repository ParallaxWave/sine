var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("clojure", require("highlight.js/lib/languages/clojure.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "clojure",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "clojure" },
      React.createElement('code', {
        className: 'hljs ' + "clojure",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("clojure", children, true).value
        }
      })
    );
  }
});
