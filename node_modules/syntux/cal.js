var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("cal", require("highlight.js/lib/languages/cal.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "cal",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "cal" },
      React.createElement('code', {
        className: 'hljs ' + "cal",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("cal", children, true).value
        }
      })
    );
  }
});
