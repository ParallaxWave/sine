var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("groovy", require("highlight.js/lib/languages/groovy.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "groovy",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "groovy" },
      React.createElement('code', {
        className: 'hljs ' + "groovy",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("groovy", children, true).value
        }
      })
    );
  }
});
