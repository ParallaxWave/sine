var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("vbnet", require("highlight.js/lib/languages/vbnet.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "vbnet",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "vbnet" },
      React.createElement('code', {
        className: 'hljs ' + "vbnet",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("vbnet", children, true).value
        }
      })
    );
  }
});
