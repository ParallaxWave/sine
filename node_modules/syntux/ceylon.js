var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("ceylon", require("highlight.js/lib/languages/ceylon.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "ceylon",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "ceylon" },
      React.createElement('code', {
        className: 'hljs ' + "ceylon",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("ceylon", children, true).value
        }
      })
    );
  }
});
