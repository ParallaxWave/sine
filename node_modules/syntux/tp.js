var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("tp", require("highlight.js/lib/languages/tp.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "tp",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "tp" },
      React.createElement('code', {
        className: 'hljs ' + "tp",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("tp", children, true).value
        }
      })
    );
  }
});
