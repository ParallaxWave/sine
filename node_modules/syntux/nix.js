var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("nix", require("highlight.js/lib/languages/nix.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "nix",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "nix" },
      React.createElement('code', {
        className: 'hljs ' + "nix",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("nix", children, true).value
        }
      })
    );
  }
});
