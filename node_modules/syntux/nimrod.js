var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("nimrod", require("highlight.js/lib/languages/nimrod.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "nimrod",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "nimrod" },
      React.createElement('code', {
        className: 'hljs ' + "nimrod",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("nimrod", children, true).value
        }
      })
    );
  }
});
