var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("dns", require("highlight.js/lib/languages/dns.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "dns",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "dns" },
      React.createElement('code', {
        className: 'hljs ' + "dns",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("dns", children, true).value
        }
      })
    );
  }
});
