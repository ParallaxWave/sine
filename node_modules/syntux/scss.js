var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("scss", require("highlight.js/lib/languages/scss.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "scss",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "scss" },
      React.createElement('code', {
        className: 'hljs ' + "scss",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("scss", children, true).value
        }
      })
    );
  }
});
