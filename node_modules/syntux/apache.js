var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("apache", require("highlight.js/lib/languages/apache.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "apache",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "apache" },
      React.createElement('code', {
        className: 'hljs ' + "apache",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("apache", children, true).value
        }
      })
    );
  }
});
