var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("gcode", require("highlight.js/lib/languages/gcode.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "gcode",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "gcode" },
      React.createElement('code', {
        className: 'hljs ' + "gcode",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("gcode", children, true).value
        }
      })
    );
  }
});
