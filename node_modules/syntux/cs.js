var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("cs", require("highlight.js/lib/languages/cs.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "cs",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "cs" },
      React.createElement('code', {
        className: 'hljs ' + "cs",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("cs", children, true).value
        }
      })
    );
  }
});
