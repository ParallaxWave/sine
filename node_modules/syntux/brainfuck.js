var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("brainfuck", require("highlight.js/lib/languages/brainfuck.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "brainfuck",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "brainfuck" },
      React.createElement('code', {
        className: 'hljs ' + "brainfuck",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("brainfuck", children, true).value
        }
      })
    );
  }
});
