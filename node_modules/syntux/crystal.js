var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("crystal", require("highlight.js/lib/languages/crystal.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "crystal",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "crystal" },
      React.createElement('code', {
        className: 'hljs ' + "crystal",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("crystal", children, true).value
        }
      })
    );
  }
});
