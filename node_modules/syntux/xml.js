var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("xml", require("highlight.js/lib/languages/xml.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "xml",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "xml" },
      React.createElement('code', {
        className: 'hljs ' + "xml",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("xml", children, true).value
        }
      })
    );
  }
});
