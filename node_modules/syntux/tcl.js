var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("tcl", require("highlight.js/lib/languages/tcl.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "tcl",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "tcl" },
      React.createElement('code', {
        className: 'hljs ' + "tcl",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("tcl", children, true).value
        }
      })
    );
  }
});
