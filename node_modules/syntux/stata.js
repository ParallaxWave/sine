var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("stata", require("highlight.js/lib/languages/stata.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "stata",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "stata" },
      React.createElement('code', {
        className: 'hljs ' + "stata",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("stata", children, true).value
        }
      })
    );
  }
});
