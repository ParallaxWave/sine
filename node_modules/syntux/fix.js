var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("fix", require("highlight.js/lib/languages/fix.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "fix",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "fix" },
      React.createElement('code', {
        className: 'hljs ' + "fix",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("fix", children, true).value
        }
      })
    );
  }
});
