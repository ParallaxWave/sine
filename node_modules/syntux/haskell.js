var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("haskell", require("highlight.js/lib/languages/haskell.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "haskell",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "haskell" },
      React.createElement('code', {
        className: 'hljs ' + "haskell",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("haskell", children, true).value
        }
      })
    );
  }
});
