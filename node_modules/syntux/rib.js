var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("rib", require("highlight.js/lib/languages/rib.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "rib",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "rib" },
      React.createElement('code', {
        className: 'hljs ' + "rib",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("rib", children, true).value
        }
      })
    );
  }
});
