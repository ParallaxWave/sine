var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("haml", require("highlight.js/lib/languages/haml.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "haml",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "haml" },
      React.createElement('code', {
        className: 'hljs ' + "haml",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("haml", children, true).value
        }
      })
    );
  }
});
