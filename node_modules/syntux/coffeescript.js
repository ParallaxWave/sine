var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("coffeescript", require("highlight.js/lib/languages/coffeescript.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "coffeescript",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "coffeescript" },
      React.createElement('code', {
        className: 'hljs ' + "coffeescript",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("coffeescript", children, true).value
        }
      })
    );
  }
});
