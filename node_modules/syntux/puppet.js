var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("puppet", require("highlight.js/lib/languages/puppet.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "puppet",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "puppet" },
      React.createElement('code', {
        className: 'hljs ' + "puppet",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("puppet", children, true).value
        }
      })
    );
  }
});
