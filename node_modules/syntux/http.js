var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("http", require("highlight.js/lib/languages/http.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "http",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "http" },
      React.createElement('code', {
        className: 'hljs ' + "http",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("http", children, true).value
        }
      })
    );
  }
});
