var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("golo", require("highlight.js/lib/languages/golo.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "golo",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "golo" },
      React.createElement('code', {
        className: 'hljs ' + "golo",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("golo", children, true).value
        }
      })
    );
  }
});
