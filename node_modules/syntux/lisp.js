var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("lisp", require("highlight.js/lib/languages/lisp.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "lisp",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "lisp" },
      React.createElement('code', {
        className: 'hljs ' + "lisp",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("lisp", children, true).value
        }
      })
    );
  }
});
