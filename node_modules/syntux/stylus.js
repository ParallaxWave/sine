var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("stylus", require("highlight.js/lib/languages/stylus.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "stylus",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "stylus" },
      React.createElement('code', {
        className: 'hljs ' + "stylus",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("stylus", children, true).value
        }
      })
    );
  }
});
