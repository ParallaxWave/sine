var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("gherkin", require("highlight.js/lib/languages/gherkin.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "gherkin",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "gherkin" },
      React.createElement('code', {
        className: 'hljs ' + "gherkin",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("gherkin", children, true).value
        }
      })
    );
  }
});
