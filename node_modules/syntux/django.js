var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("django", require("highlight.js/lib/languages/django.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "django",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "django" },
      React.createElement('code', {
        className: 'hljs ' + "django",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("django", children, true).value
        }
      })
    );
  }
});
