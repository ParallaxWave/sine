var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("typescript", require("highlight.js/lib/languages/typescript.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "typescript",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "typescript" },
      React.createElement('code', {
        className: 'hljs ' + "typescript",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("typescript", children, true).value
        }
      })
    );
  }
});
