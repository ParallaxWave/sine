var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("dos", require("highlight.js/lib/languages/dos.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "dos",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "dos" },
      React.createElement('code', {
        className: 'hljs ' + "dos",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("dos", children, true).value
        }
      })
    );
  }
});
