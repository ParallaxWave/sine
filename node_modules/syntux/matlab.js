var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("matlab", require("highlight.js/lib/languages/matlab.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "matlab",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "matlab" },
      React.createElement('code', {
        className: 'hljs ' + "matlab",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("matlab", children, true).value
        }
      })
    );
  }
});
