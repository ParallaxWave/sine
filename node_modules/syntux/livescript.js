var React = require('react');
var hljs = require('highlight.js/lib/highlight.js');
hljs.registerLanguage("livescript", require("highlight.js/lib/languages/livescript.js"));

module.exports = React.createClass({
  displayName: 'HighLight-' + "livescript",
  render: function () {
    var children = this.props.children
    if (typeof children !== 'string') children = React.Children.only(children)
    return React.createElement('pre', { className: 'hljs ' + "livescript" },
      React.createElement('code', {
        className: 'hljs ' + "livescript",
        dangerouslySetInnerHTML: {
          __html: hljs.highlight("livescript", children, true).value
        }
      })
    );
  }
});
